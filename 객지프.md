# 상향/하향 형 변환
상향 형변환(upcasting): 자동, 자식 클래스 타입을 부모 클래스 타입으로 변환, 자식 멤버 접근불가
case1.
child p;
parent* q = &p;
parent& q = p;
하향 형변환(downcasting): 부모 클래스 타입을 자식 클래스 타입으로 변환, 자식 public 접근가능
parent* p = new child(); //상향 형변환
case1.
child* q = static_cast<child*>(p);
child* q = dynamic_cast<child*>(p); //실패시 nullptr 반환
case2.
parent* q = (parent*)p;
(parent*)p->setwidth();
# 다형성
다형성: 객체의 타입이 다르면 똑같은 메시지가 전달되더라도 서로 다른 동작을 함, 하나의 코드로 다양한 타입의 객체를 처리
함수의 매개 변수로는 부모 클래스 타입을 선언하는 것이 좋다.
정적 바인딩: 컴파일 단계에서 모든 바인딩이 완료, 빠름, 일반 함수
동적 바인딩: 실행 시간에 실제 호출되는 함수를 결정, 지연 바인딩, 느림, 가상 함수
참조자인 경우에도 다형성이 동작
가상 소멸자: 다형성을 사용하는 과정에서 소멸자를 virtual로 해주지 않으면 문제 발생
순수 가상 함수: 함수 헤더만 존재하고 몸체는 없는 함수, virtual int move() = 0;
추상 클래스: 순수 가상함수를 하나라도 가지고 있는 클래스
추상 클래스는 객체들 사이에 상호작용하기 위한 인터페이스를 정의하는 용도로 사용 가능
# a++/++a 차이
전위 연산자: 매개변수 없음, operator++()
vector& operator++() {
    x++;
    y++;
    return *this;
}
후위 연산자: 더미 인자로 구별, operator++(int)
const vector operator++(int){
    vector temp = *this;
    x++;
    y++;
    return temp;
}
# try catch
상황: 잘못된 코드, 부정확한 데이터, 예외적 상황에 의한 런타임 오류(0으로 나누기, 배열 인덱스 초과, 하드웨어 에러)
catch: 예외 처리, 항상 구체적 타입 먼저, catch(...)는 마지막
사용자 정의 예외 클래스: 자식클래스 throw, 부모클래스 catch, 다형성을 이용해 자식 throw함수 실행
# 벡터
동적 배열, #include<vector> 필요
push_back(): 맨뒤 추가
pop_back(): 맨뒤 삭제
size(): int, 개수
empty(): bool, 비어있음 여부
vector<int>::iterator it: 반복자, 배열 내부 순회
begin(): 첫 번째 주소(반복자)
end(): 마지막 + 1 주소(반복자)
v[i]: i번쨰 원소
전향 반복자: ++, forward_list
양방향 반복자: ++ --, list, set, map
무작위 접근 반복자: ++ -- [], vector, deque
# friend
프렌드 함수: 클래스의 내부 데이터에 접근할 수 있는 특수한 함수
프렌드 함수의 원형은 클래스 안에 포함 but 멤버 함수는 아님, 본체는 외부에서 따로 정의, 클래스 내부 모든 멤버 변수 사용 가능(private 포함)
클래스도 프렌드로 선언 가능, 두 개의 객체를 비교할 때 많이 사용
# 함수/클래스 템플릿
함수 템플릿: 함수를 찍어내기 위한 형틀
클래스 템플릿: 클래스를 찍어내는 틀
일반화: 일반적인 코드를 작성하고 이 코드를 다양한 타입의 객체에 대하여 재사용
일반화: 자료형에 무관하게 동작하는 함수 설계, template<typename T>, template<class T>
특수화: 특정 타입에 별도 구현, template<>
일반 함수와 템플릿 공존 시 일반 함수 우선
일반화
template<typename T>
class Box {
    T value;
};
Box<int> b1;
특수화
template<>
const char* getmax<const char*>(const char* a, const char* b){
    return (strcmp(a, b) > 0) ? a : b;
}
stack: last in first out
push(): 맨뒤 추가, stack[++top] = 10;
pop(): 맨뒤 삭제, value = stack[top--];
isEmpty(): bool, 비어있음 여부, top == -1;
isFull(): bool, 가득참 여부, top == 배열크기 - 1;
# 가상함수/포인터
#include <iostream>
using namespace std;

class Parent {
public:
    virtual void vfunc() { cout << "Parent virtual" << endl; }
    void func() { cout << "Parent normal" << endl; }
};

class Child : public Parent {
public:
    void vfunc() override { cout << "Child virtual" << endl; }
    void func() { cout << "Child normal" << endl; }
};

int main() {
    Parent p;
    Child c;

    // 1. 일반 객체 호출
    p.vfunc();   // Parent virtual
    p.func();    // Parent normal
    c.vfunc();   // Child virtual
    c.func();    // Child normal

    // 2. 상향 형변환 (Parent* → Child 객체)
    Parent* ptr = &c;
    ptr->vfunc();  // Child virtual  ← 가상함수: 자식 호출
    ptr->func();   // Parent normal  ← 일반함수: 부모 호출

    // 3. 하향 형변환 (Child* → Child 객체)
    Child* cptr = static_cast<Child*>(ptr);
    cptr->vfunc();  // Child virtual
    cptr->func();   // Child normal

    // 4. 부모 포인터 → 부모 객체
    Parent* pptr = &p;
    pptr->vfunc();  // Parent virtual
    pptr->func();   // Parent normal

    // 5. dynamic_cast 실패
    Parent* pptr2 = new Parent();
    Child* cptr2 = dynamic_cast<Child*>(pptr2);
    if (cptr2 == nullptr) cout << "dynamic_cast 실패" << endl;  // 실패

    // 6. dynamic_cast 성공
    Parent* pptr3 = new Child();
    Child* cptr3 = dynamic_cast<Child*>(pptr3);
    if (cptr3) cptr3->vfunc();  // Child virtual

    return 0;
}
# 함수 템플릿, 벡터
#include <iostream>
#include <vector>
using namespace std;

// 1. 일반화 함수 템플릿
template<typename T>
T getmax(T a, T b) {
    return (a > b) ? a : b;
}

// 2. 특수화
template<>
const char* getmax<const char*>(const char* a, const char* b) {
    return (strcmp(a, b) > 0) ? a : b;
}

// 3. 일반 함수 (템플릿보다 우선)
int getmax(int a, int b) {
    cout << "일반 함수 호출" << endl;
    return (a > b) ? a : b;
}

// 4. 클래스 템플릿
template<typename T>
class Box {
    T value;
public:
    Box(T v) : value(v) {}
    T get() { return value; }
};

// 5. 벡터를 매개변수로 받는 템플릿
template<typename T>
void printVector(vector<T> v) {
    for (int i = 0; i < v.size(); i++)
        cout << v[i] << " ";
    cout << endl;
}

int main() {
    // 함수 템플릿
    cout << getmax(3.5, 2.1) << endl;       // 3.5 (일반화)
    cout << getmax("banana", "apple") << endl; // banana (특수화)
    cout << getmax(1, 2) << endl;            // 일반 함수 호출, 2

    // 클래스 템플릿
    Box<int> b1(10);
    Box<string> b2("hello");
    cout << b1.get() << endl;   // 10
    cout << b2.get() << endl;   // hello

    // 벡터 기본
    vector<int> v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);   // [1, 2, 3]
    cout << v.size()  << endl;  // 3
    cout << v.empty() << endl;  // 0
    v.pop_back();               // [1, 2]
    cout << v.size()  << endl;  // 2

    // 벡터 + 템플릿
    printVector(v);              // 1 2

    vector<string> vs;
    vs.push_back("hello");
    vs.push_back("world");
    printVector(vs);             // hello world

    return 0;
}
# 예외처리(범위 밖 있음), 파일 핸들링
#include <iostream>
#include <fstream>
#include <stdexcept>
using namespace std;

// 1. 기본 예외처리
void basic() {
    try {
        throw 42;
    } catch (int e) {
        cout << "int 예외: " << e << endl;
    }
}

// 2. 다중 catch
void multiCatch(int x) {
    try {
        if (x == 0) throw 0;
        if (x == 1) throw 3.14;
        if (x == 2) throw "문자열 예외";
    } catch (int e)         { cout << "int: "    << e << endl; }
      catch (double e)      { cout << "double: " << e << endl; }
      catch (const char* e) { cout << "char*: "  << e << endl; }
}

// 3. 범위 밖 예외 (전파)
void dangerous() {
    throw runtime_error("위험한 함수");
}
void propagate() {
    try {
        dangerous();  // 여기서 던짐
    } catch (runtime_error& e) {
        cout << "전파된 예외: " << e.what() << endl;
    }
}

// 4. catch(...) 모든 예외
void catchAll() {
    try {
        throw "unknown";
    } catch (...) {
        cout << "모든 예외 처리" << endl;
    }
}

// 5. 사용자 정의 예외
class MyException : public exception {
public:
    const char* what() const noexcept override {
        return "사용자 정의 예외";
    }
};
void customEx() {
    try {
        throw MyException();
    } catch (MyException& e) {
        cout << e.what() << endl;
    }
}

// 6. 파일 쓰기
void fileWrite() {
    ofstream ofs("test.txt");
    if (!ofs) {
        cout << "파일 열기 실패" << endl;
        return;
    }
    ofs << "hello" << endl;
    ofs << "world" << endl;
    ofs.close();
}

// 7. 파일 읽기
void fileRead() {
    ifstream ifs("test.txt");
    if (!ifs) {
        cout << "파일 열기 실패" << endl;
        return;
    }
    string line;
    while (getline(ifs, line))
        cout << line << endl;
    ifs.close();
}

// 8. 파일 + 예외처리
void fileWithException() {
    try {
        ifstream ifs("none.txt");
        if (!ifs) throw runtime_error("파일 없음");
        string line;
        while (getline(ifs, line))
            cout << line << endl;
        ifs.close();
    } catch (runtime_error& e) {
        cout << "예외: " << e.what() << endl;
    }
}

int main() {
    basic();           // int 예외: 42
    multiCatch(0);     // int: 0
    multiCatch(1);     // double: 3.14
    multiCatch(2);     // char*: 문자열 예외
    propagate();       // 전파된 예외: 위험한 함수
    catchAll();        // 모든 예외 처리
    customEx();        // 사용자 정의 예외
    fileWrite();       // test.txt 생성
    fileRead();        // hello world 출력
    fileWithException(); // 예외: 파일 없음

    return 0;
}